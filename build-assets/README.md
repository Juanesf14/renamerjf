# Coloca aquí los iconos del app:
# icon.icns  → macOS (1024x1024, formato .icns)
# icon.ico   → Windows (256x256, formato .ico)
# icon.png   → Linux (512x512, formato .png)
#
# Para generar desde un PNG usa:
#   brew install librsvg
#   electron-icon-builder --input=icon.png --output=build-assets
